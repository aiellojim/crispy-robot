# 實作委派模板（複製後填空）

```
## 背景
（1–3 句：這個改動屬於哪個功能、為什麼要做。相關檔案位置先查 docs/jsx-map.md 填進來。）

## 目標
（一句話說清楚完成後的行為。例：「NotificationPanel 開啟時自動從瀏覽器 endpoint 恢復訂閱勾選狀態」。）

## 限制
- 適用的 lessons 條目：#__（先查 docs/lessons.md，把編號抄進來；動 JSONB 必含 #1，動 auth 必含 #2–#4）
- 禁止整檔讀取 src/hotel-project-dashboard.jsx，用 docs/jsx-map.md + grep 定位
- 禁止把 secret 寫進任何會 commit 的檔案
- （其他：不准動哪些檔、要沿用哪個既有模式…）

## 驗收條件
- （可檢查的判準，逐條列。例：「npm run build 通過」「重開頁面後面板勾選狀態與 push_subscriptions 一致」）

## 回報格式
- 改了哪些檔案（路徑 + 大意）
- 驗收條件逐條回報 ✅/❌ 與證據
- 遇到的意外與未解事項（沒有就寫「無」）
```
